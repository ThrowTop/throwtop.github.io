Read `SITE.md` before editing this project.

For most tasks, the sections "Project Layout", "Build and Deploy", and
"Backup Script Release Flow" are enough. Read the whole file only when changing
the build system, GitHub Pages deployment, or the backup script release path.
